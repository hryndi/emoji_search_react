export type TEmojiData = {
  code: string;
  character: string;
  image: string;
  name: string;
  group: string;
  subgroup: string;
};

export type StateData = {
  character: string;
  name: string;
};
